import { HiOutlineLink } from 'react-icons/hi';
import ComingSoon from '@/components/ui/ComingSoon';

export default function LinksPage() {
  return (
    <ComingSoon
      title="Links"
      description="Generate shareable product links and payment links to send via WhatsApp."
      icon={HiOutlineLink}
    />
  );
}
